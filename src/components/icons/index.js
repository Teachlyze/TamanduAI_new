// src/components/icons/index.js
import { Check, ArrowLeft, Loader2 } from 'lucide-react';

// Using Lucide React icons for better compatibility
export const CheckIcon = Check;
export const ArrowBackIcon = ArrowLeft;
export const RefreshIcon = Loader2;

// Icons object for convenient access
export const Icons = {
  check: CheckIcon,
  arrowLeft: ArrowBackIcon,
  spinner: RefreshIcon,
};
